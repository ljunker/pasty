services:
  pasty:
    image: kryptikker/pasty:1.0.0
    container_name: pasty
    restart: unless-stopped

    ports:
      - "127.0.0.1:41237:8000"

    volumes:
      - ./data/uploads:/app/uploads
      - ./data/tiny-pastebin.db:/app/tiny-pastebin.db